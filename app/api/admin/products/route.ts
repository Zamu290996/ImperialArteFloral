import {prisma} from '@/lib/prisma';import {getSessionUserId} from '@/lib/auth';import {NextResponse} from 'next/server';export async function POST(req:Request){if(!await getSessionUserId())return NextResponse.json({error:'No autorizado'},{status:401});const f=await req.formData();await prisma.product.create({data:{name:String(f.get('name')||''),description:String(f.get('description')||'')||null,category:String(f.get('category'))==='FUNERAL_WREATH'?'FUNERAL_WREATH':'COMMERCIAL',price:Number(f.get('price')||0)}});return NextResponse.redirect(new URL('/admin/productos',req.url),303)}