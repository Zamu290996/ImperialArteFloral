import {prisma} from '@/lib/prisma'; import {getSessionUserId} from '@/lib/auth'; import {redirect} from 'next/navigation'; export const dynamic='force-dynamic';
export default async function Admin(){if(!await getSessionUserId())redirect('/admin/login');const [orders,products]=await Promise.all([prisma.order.findMany({include:{items:{include:{product:true}}},orderBy:{createdAt:'desc'},take:25}),prisma.product.count({where:{active:true}})]);return <main className="section"><h1>Panel interno</h1><div className="grid"><div className="card"><div className="muted">Pedidos</div><div className="kpi">{orders.length}</div></div><div className="card"><div className="muted">Pagados</div><div className="kpi">{orders.filter(o=>o.paymentStatus==='PAID').length}</div></div><div className="card"><div className="muted">Productos</div><div className="kpi">{products}</div></div></div><div style={{margin:'18px 0'}}><a className="btn secondary" href="/admin/productos">Productos</a> <a className="btn secondary" href="/admin/conmutador">Conmutador</a></div><div className="card"><h2>Pedidos recientes</h2><table className="table"><thead><tr><th>Folio</th><th>Cliente</th><th>Producto</th><th>Pago</th><th>Estado</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>{o.folio}</td><td>{o.customerName}<br/>{o.phone}</td><td>{o.items[0]?.product.name}</td><td>{o.paymentStatus}</td><td>{o.status}</td></tr>)}</tbody></table></div><form action="/api/auth/logout" method="post" style={{marginTop:16}}><button className="btn secondary">Cerrar sesión</button></form></main>}
