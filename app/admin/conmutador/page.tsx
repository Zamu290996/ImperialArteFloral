import {prisma} from '@/lib/prisma'
import {getSessionUserId} from '@/lib/auth'
import {redirect} from 'next/navigation'
import {pbxMode,getPublicNumber} from '@/lib/pbx'
import Link from 'next/link'
export const dynamic='force-dynamic'
function Side(){return <aside className="admin-sidebar"><div className="admin-title">Imperial Admin</div><div className="admin-sub">Centro de operación</div><nav className="admin-menu"><Link href="/admin">▦ Resumen</Link><Link href="/admin/productos">✦ Productos</Link><Link href="/admin/conmutador">☎ Conmutador</Link><Link href="/inicio">← Volver al sitio</Link></nav></aside>}
export default async function C(){if(!await getSessionUserId())redirect('/admin/login');const logs=await prisma.callLog.findMany({orderBy:{createdAt:'desc'},take:50});return <main className="admin-layout"><Side/><section className="admin-content"><div className="admin-topbar"><div><span className="eyebrow">Telefonía</span><h1 style={{marginTop:12}}>Conmutador</h1><p className="muted">Preparado para integrar un proveedor VoIP/PBX externo.</p></div><Link className="btn secondary small" href="/admin">← Volver</Link></div><div className="notice">Para activar telefonía real faltan proveedor, número virtual, extensiones, IVR y credenciales del servicio.</div><div className="kpi-grid" style={{marginTop:18}}><div className="kpi-card"><div className="kpi-label">Modo</div><div style={{fontWeight:800,marginTop:8}}>{pbxMode()}</div></div><div className="kpi-card"><div className="kpi-label">Número público</div><div style={{fontWeight:800,marginTop:8}}>{getPublicNumber()}</div></div><div className="kpi-card"><div className="kpi-label">Llamadas registradas</div><div className="kpi">{logs.length}</div></div></div><div className="card flat"><h2>Actividad reciente</h2>{logs.length===0?<div className="empty">Aún no hay llamadas registradas.</div>:<div className="table-wrap"><table className="table"><thead><tr><th>Fecha</th><th>Origen</th><th>Destino</th><th>Estado</th><th>Duración</th></tr></thead><tbody>{logs.map(l=><tr key={l.id}><td>{new Date(l.createdAt).toLocaleString('es-MX')}</td><td>{l.caller}</td><td>{l.destination}</td><td><span className="status">{l.status}</span></td><td>{l.duration?`${l.duration}s`:'—'}</td></tr>)}</tbody></table></div>}</div></section></main>}
