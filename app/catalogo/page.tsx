import {prisma} from '@/lib/prisma'; export const dynamic='force-dynamic';
export default async function Catalogo(){const ps=await prisma.product.findMany({where:{active:true}});return <main className="section"><h1>Catálogo</h1><div className="grid">{ps.map(p=><article className="card" key={p.id}><span className="tag">{p.category==='FUNERAL_WREATH'?'Corona fúnebre':'Arreglo comercial'}</span><h2>{p.name}</h2><p>{p.description}</p><div className="price">${Number(p.price).toLocaleString('es-MX')} MXN</div><a className="btn" href={`/pedido?product=${p.id}`}>Personalizar</a></article>)}</div></main>}
